(function() {

    //opens modal right after window starts
    //opens if document.cookie is bigger than 0 and protocol is https

    //if (document.cookie.indexOf("vendors") < 0
  //*&& location.protocol == "https:"*/) {}
      document.getElementById('bd').classList.add("noscroll");
      MyApp.createModal();

}())
